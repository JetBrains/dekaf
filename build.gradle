apply plugin: 'java'

repositories {
    mavenCentral()
}

configurations {
    lib {

    }
}

dependencies {
    lib "org.jetbrains:annotations:15.0",
        "org.jetbrains:annotations:15.0:sources",
        "org.jetbrains:annotations:15.0:javadoc"
}

task getLibs(type:Copy) {
    from configurations.lib
    into "lib/"
}
